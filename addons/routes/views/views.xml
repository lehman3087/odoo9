<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="routes.list">
      <field name="name">routes list</field>
      <field name="model">routes.routes</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="routes.action_window">
      <field name="name">routes window</field>
      <field name="res_model">routes.routes</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="routes.action_server">
      <field name="name">routes server</field>
      <field name="model_id" ref="model_routes_routes"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="routes" id="routes.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="routes.menu_1" parent="routes.menu_root"/>
    <menuitem name="Menu 2" id="routes.menu_2" parent="routes.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="routes.menu_1_list" parent="routes.menu_1"
              action="routes.action_window"/>
    <menuitem name="Server to list" id="routes" parent="routes.menu_2"
              action="routes.action_server"/>
    -->
  </data>
</openerp>